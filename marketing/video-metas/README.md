# Video "Cargá tus metas en BIP" · 39 s · 16:9

Abrí "Configuración de objetivos" en un tablero (KPIs "sin meta") → meta del mes por KPI + grilla de 12 meses →
detalle (dirección, verde/amarillo desde %) → Guardar → real vs meta (card Mes/YTD con semáforo + barras real azul vs
meta gris) → Seguimiento de objetivos → cierre "Listo. Tus metas, en marcha." Réplica de
`components/web/meta-panel.tsx` de BIP. Datos ilustrativos. Mismo motor que los otros videos.

Render: `NODE_PATH=/opt/node22/lib/node_modules PATH=$HOME/bin:$PATH node render.mjs` → `export/metas.mp4`.
En BIP: `public/videos/metas.*`, dentro del MetaPanel (al expandir "Configuración de objetivos").
