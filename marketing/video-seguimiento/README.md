# Video "Seguimiento de objetivos en BIP" · 41 s · 16:9

Primero lo que tiene que estar cargado (tableros con datos e histórico · Mapa con objetivos, planes y KPIs · metas de
cada KPI), después cómo se arma (KPI = real ÷ meta; objetivo = Σ peso × cumplimiento; global = Σ peso del objetivo ×
cumplimiento) y cómo se lee (Cumplimiento Global + objetivos con semáforo + tabla Real/Meta mes y YTD con desvío y
evolución; el rojo = dónde actuar). Réplica de `components/objetivos/{objetivos-hero,kpi-scorecard}.tsx` de BIP.

Render: `NODE_PATH=/opt/node22/lib/node_modules PATH=$HOME/bin:$PATH node render.mjs` → `export/seguimiento.mp4`.
En BIP: `public/videos/seguimiento.*`, en /overview (Seguimiento Objetivos).
